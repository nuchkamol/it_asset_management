module.exports = [
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[project]/apps/web/app/(app)/layout.tsx [app-rsc] (ecmascript, Next.js Server Component)", ((__turbopack_context__) => {

__turbopack_context__.n(__turbopack_context__.i("[project]/apps/web/app/(app)/layout.tsx [app-rsc] (ecmascript)"));
}),
"[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (client reference proxy) <module evaluation>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
// This file is generated by next-core EcmascriptClientReferenceModule.
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server.js [app-rsc] (ecmascript)");
;
const __TURBOPACK__default__export__ = (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call the default export of [project]/apps/web/components/ui/BackButton.tsx <module evaluation> from the server, but it's on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/apps/web/components/ui/BackButton.tsx <module evaluation>", "default");
}),
"[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (client reference proxy)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
// This file is generated by next-core EcmascriptClientReferenceModule.
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server.js [app-rsc] (ecmascript)");
;
const __TURBOPACK__default__export__ = (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call the default export of [project]/apps/web/components/ui/BackButton.tsx from the server, but it's on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/apps/web/components/ui/BackButton.tsx", "default");
}),
"[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$ui$2f$BackButton$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__$3c$module__evaluation$3e$__ = __turbopack_context__.i("[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (client reference proxy) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$ui$2f$BackButton$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__ = __turbopack_context__.i("[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (client reference proxy)");
;
__turbopack_context__.n(__TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$ui$2f$BackButton$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__);
}),
"[project]/apps/web/services/licenses.service.mock.ts [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// src/services/licenses.service.mock.ts
//   ใช้ alias path ของโปรเจกต์ (ปรับให้ตรงกับโปรเจกต์คุณ เช่น "@/types")
__turbopack_context__.s([
    "consumeSeat",
    ()=>consumeSeat,
    "getAvailableLicenses",
    ()=>getAvailableLicenses,
    "getLicenseById",
    ()=>getLicenseById,
    "getLicenseSummary",
    ()=>getLicenseSummary,
    "getLicenses",
    ()=>getLicenses,
    "releaseSeat",
    ()=>releaseSeat
]);
// -------------------------------------------------
// Types ที่ใช้ภายในไฟล์นี้
// -------------------------------------------------
/** สรุป (ไม่ผูกกับ paging) */ // -------------------------------------------------
// Mock dictionaries
// -------------------------------------------------
const manufacturers = [
    "Microsoft",
    "Adobe",
    "Autodesk",
    "JetBrains",
    "Atlassian"
];
const licenseModels = [
    "Per-User",
    "Perpetual",
    "Subscription",
    "Per-Device"
];
const statuses = [
    "Active",
    "Expired"
]; // เพิ่ม "Expiring" ใน type ได้ในอนาคต
// สำหรับ compliance: อ้างจากฝั่ง software ถ้าคุณมี enum กลาง ให้ import มาแทน
const complianceOptions = [
    "Compliant",
    "Non-Compliant",
    "Warning"
];
// -------------------------------------------------
// Helpers
// -------------------------------------------------
const normalize = (s)=>(s ?? "").trim().toLowerCase();
const SORT_KEYS = [
    "softwareName",
    "manufacturer",
    "licenseModel",
    "total",
    "inUse",
    "available",
    "expiryDate",
    "status",
    "compliance"
];
function isSortKey(k) {
    return !!k && SORT_KEYS.includes(k);
}
function compare(key, a, b) {
    const va = a[key];
    const vb = b[key];
    // date
    if (key === "expiryDate") {
        const ta = va ? new Date(`${va}T23:59:59Z`).getTime() : Number.NaN;
        const tb = vb ? new Date(`${vb}T23:59:59Z`).getTime() : Number.NaN;
        if (Number.isNaN(ta) && Number.isNaN(tb)) return 0;
        if (Number.isNaN(ta)) return 1;
        if (Number.isNaN(tb)) return -1;
        return ta - tb;
    }
    // number
    if (typeof va === "number" && typeof vb === "number") return va - vb;
    // string
    return String(va ?? "").localeCompare(String(vb ?? ""), undefined, {
        numeric: true,
        sensitivity: "base"
    });
}
function safeTime(d) {
    if (!d) return Number.NaN;
    const t = new Date(`${d}T23:59:59Z`).getTime();
    return Number.isNaN(t) ? Number.NaN : t;
}
// -------------------------------------------------
// Mock dataset ให้สอดคล้องกับ LicenseItem ของคุณ
// -------------------------------------------------
const MOCK_LICENSES = Array.from({
    length: 57
}).map((_, i)=>{
    const total = 100 + i % 7 * 5;
    const inUse = 40 + i % 5 * 7;
    const available = Math.max(0, total - inUse);
    return {
        id: `LIC-${i + 1}`,
        softwareName: `Software ${i + 1}`,
        manufacturer: manufacturers[i % manufacturers.length],
        licenseModel: licenseModels[i % licenseModels.length],
        // ฟิลด์อื่น ๆ ตาม LicenseItem ของคุณ:
        sku: `SKU-${i + 1}`,
        edition: undefined,
        version: `v${i % 5 + 1}.0`,
        consumptionUnit: "perUser",
        term: "subscription",
        total,
        inUse,
        available,
        expiryDate: `2026-${String(i % 12 + 1).padStart(2, "0")}-28`,
        status: i % 3 === 0 ? "Expired" : "Active",
        compliance: i % 4 === 0 ? "Warning" : "Compliant",
        // metadata
        createdAt: `2025-01-${String(i % 28 + 1).padStart(2, "0")}T12:00:00Z`,
        updatedAt: `2025-12-${String(i % 28 + 1).padStart(2, "0")}T12:00:00Z`,
        cost: 10000 + i * 137,
        maintenanceCost: 1000 + i % 10 * 37,
        notes: "",
        licenseKey: `XXXX-XXXX-XXXX-${String(i + 1).padStart(4, "0")}`
    };
});
async function getLicenseById(id) {
    await new Promise((r)=>setTimeout(r, 120));
    return MOCK_LICENSES.find((x)=>String(x.id) === String(id)) ?? null;
}
async function getLicenses(q) {
    return new Promise((resolve)=>{
        setTimeout(()=>{
            const kw = normalize(q.search);
            let data = [
                ...MOCK_LICENSES
            ];
            // filters
            if (q.status) data = data.filter((x)=>normalize(x.status) === normalize(q.status));
            if (q.licenseModel) data = data.filter((x)=>normalize(x.licenseModel) === normalize(q.licenseModel));
            if (q.manufacturer) data = data.filter((x)=>normalize(x.manufacturer) === normalize(q.manufacturer));
            if (kw) {
                data = data.filter((x)=>normalize(x.softwareName).includes(kw) || normalize(x.manufacturer).includes(kw) || normalize(x.licenseModel).includes(kw));
            }
            //   sort (global ก่อน paginate)
            const dir = q.sortOrder === "desc" ? -1 : 1;
            const key = isSortKey(q.sortBy) ? q.sortBy : undefined;
            if (q.sortBy === "status_priority" && !q.status /* All Status */ ) {
                // Priority: Active -> Expiring -> Expired -> else
                const pr = new Map([
                    [
                        "Active",
                        0
                    ],
                    [
                        "Expiring",
                        1
                    ],
                    [
                        "Expired",
                        2
                    ]
                ]);
                data.sort((a, b)=>{
                    const pa = pr.get(a.status) ?? 999;
                    const pb = pr.get(b.status) ?? 999;
                    if (pa !== pb) return (pa - pb) * dir;
                    // secondary: ใช้ softwareName (อ่านง่ายและเสถียร)
                    return String(a.softwareName ?? "").localeCompare(String(b.softwareName ?? ""), undefined, {
                        numeric: true,
                        sensitivity: "base"
                    });
                });
            } else if (key) {
                // generic field sort
                data.sort((a, b)=>compare(key, a, b) * dir);
            } else {
                // default: softwareName ASC
                data.sort((a, b)=>String(a.softwareName ?? "").localeCompare(String(b.softwareName ?? ""), undefined, {
                        numeric: true,
                        sensitivity: "base"
                    }));
            }
            // paginate (1-based)
            const start = (q.page - 1) * q.pageSize;
            const items = data.slice(start, start + q.pageSize);
            //   รูปแบบตอบกลับเป็น OffsetPage<LicenseItem>
            const totalCount = data.length;
            const totalPages = Math.max(1, Math.ceil(totalCount / q.pageSize));
            const page = q.page;
            const pageSize = q.pageSize;
            resolve({
                items,
                totalCount,
                page,
                pageSize,
                hasNext: page < totalPages,
                hasPrev: page > 1,
                totalPages
            });
        }, 400);
    });
}
async function getAvailableLicenses() {
    await new Promise((r)=>setTimeout(r, 100));
    return MOCK_LICENSES.filter((x)=>(x.available ?? x.total - x.inUse) > 0);
}
async function consumeSeat(licenseId, n = 1) {
    const idx = MOCK_LICENSES.findIndex((l)=>l.id === licenseId);
    if (idx < 0) throw new Error("License not found");
    const lic = {
        ...MOCK_LICENSES[idx]
    };
    const available = lic.available ?? lic.total - lic.inUse;
    if (available < n) throw new Error("No seats left");
    lic.inUse = (lic.inUse ?? 0) + n;
    lic.available = Math.max(0, lic.total - lic.inUse);
    MOCK_LICENSES[idx] = lic;
}
async function releaseSeat(licenseId, n = 1) {
    const idx = MOCK_LICENSES.findIndex((l)=>l.id === licenseId);
    if (idx < 0) return;
    const lic = {
        ...MOCK_LICENSES[idx]
    };
    lic.inUse = Math.max(0, (lic.inUse ?? 0) - n);
    lic.available = Math.max(0, lic.total - lic.inUse);
    MOCK_LICENSES[idx] = lic;
}
async function getLicenseSummary(filters, signal) {
    // รองรับ cancel
    await new Promise((resolve, reject)=>{
        const t = setTimeout(resolve, 150);
        if (signal) {
            signal.addEventListener("abort", ()=>{
                clearTimeout(t);
                reject(Object.assign(new Error("Aborted"), {
                    name: "AbortError"
                }));
            });
        }
    });
    let data = [
        ...MOCK_LICENSES
    ];
    if (filters?.status) data = data.filter((x)=>normalize(x.status) === normalize(filters.status));
    if (filters?.licenseModel) data = data.filter((x)=>normalize(x.licenseModel) === normalize(filters.licenseModel));
    if (filters?.manufacturer) data = data.filter((x)=>normalize(x.manufacturer) === normalize(filters.manufacturer));
    if (filters?.search) {
        const kw = normalize(filters.search);
        if (kw) {
            data = data.filter((x)=>normalize(x.softwareName).includes(kw) || normalize(x.manufacturer).includes(kw) || normalize(x.licenseModel).includes(kw) || normalize(x.status).includes(kw));
        }
    }
    // summary aggregation ----------------------------
    const summary = {
        total: data.length,
        active: 0,
        inactive: 0,
        expired: 0,
        warning: 0,
        available: 0,
        unknown: 0,
        seatsTotal: 0,
        seatsUsed: 0,
        seatsAvailable: 0,
        byModel: {},
        byManufacturer: {}
    };
    const nowT = Date.now();
    const soonMs = 30 * 24 * 60 * 60 * 1000; // 30 วัน
    for (const it of data){
        summary.seatsTotal += it.total ?? 0;
        summary.seatsUsed += it.inUse ?? 0;
        if (it.status === "Active") summary.active += 1;
        else if (it.status === "Expired") summary.expired += 1;
        // ถ้าเพิ่ม "Expiring" ใน LicenseStatus: ให้เพิ่มสรุปที่นี่ด้วย
        if (it.status === "Active" && (it.inUse ?? 0) === 0) {
            summary.inactive += 1;
        }
        const expT = safeTime(it.expiryDate);
        if (!Number.isNaN(expT)) {
            if (expT >= nowT && expT - nowT <= soonMs) summary.warning += 1;
        } else {
            summary.unknown += 1;
        }
        if (it.compliance === "Warning") summary.warning += 1;
        summary.byModel[it.licenseModel] = (summary.byModel[it.licenseModel] ?? 0) + 1;
        summary.byManufacturer[it.manufacturer] = (summary.byManufacturer[it.manufacturer] ?? 0) + 1;
    }
    summary.seatsAvailable = Math.max(0, summary.seatsTotal - summary.seatsUsed);
    return summary;
}
}),
"[project]/apps/web/services/assign.service.mock.ts [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ASSIGNED_TO",
    ()=>ASSIGNED_TO,
    "assignLicense",
    ()=>assignLicense,
    "assignLicenseBulk",
    ()=>assignLicenseBulk,
    "assignMany",
    ()=>assignMany,
    "getAssignedTo",
    ()=>getAssignedTo,
    "getAssignedToFilters",
    ()=>getAssignedToFilters,
    "getAssignmentsByEmployee",
    ()=>getAssignmentsByEmployee,
    "unassignLicense",
    ()=>unassignLicense
]);
// src/services/assignments.service.mock.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/services/licenses.service.mock.ts [app-rsc] (ecmascript)");
;
const _assignments = [];
const _idemSeen = new Set();
const _bulkSeen = new Set(); // idempotency สำหรับ bulk
function asPerType(licenseModel) {
    const lm = (licenseModel || "").toLowerCase();
    return lm.includes("device") ? "per_device" : "per_user";
}
function nowIso() {
    return new Date().toISOString();
}
async function assignLicense(params) {
    await delay(150);
    const { licenseId, employeeId, deviceId, idempotencyKey, effectiveAt, expiresAt } = params;
    if (!licenseId) throw new Error("licenseId is required");
    if (!employeeId) throw new Error("employeeId is required");
    // Idempotency (per-call)
    if (idempotencyKey) {
        if (_idemSeen.has(idempotencyKey)) {
            const last = _assignments.slice().reverse().find((a)=>a.employeeId === employeeId && a.licenseId === licenseId && a.deviceId === (deviceId || null));
            if (last) return last;
        } else {
            _idemSeen.add(idempotencyKey);
        }
    }
    const license = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getLicenseById"])(licenseId);
    if (!license) throw new Error("License not found");
    const perType = asPerType(license.licenseModel);
    if (perType === "per_device" && !deviceId) {
        throw new Error("device-required");
    }
    // duplicate
    const dup = _assignments.find((a)=>a.licenseId === licenseId && a.employeeId === employeeId && (perType === "per_user" ? true : a.deviceId === (deviceId || null)) && a.status === "active");
    if (dup) {
        return dup; // idempotent-ish
    }
    // seats
    const available = license.available ?? license.total - (license.inUse ?? 0);
    if (available <= 0) {
        throw new Error("Seat ไม่พอ");
    }
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["consumeSeat"])(licenseId, 1);
    const rec = {
        id: `asg-${Date.now()}-${Math.floor(Math.random() * 100000)}`,
        licenseId,
        employeeId,
        deviceId: perType === "per_device" ? deviceId || null : null,
        status: "active",
        effectiveAt: effectiveAt || nowIso(),
        expiresAt: expiresAt || null,
        createdAt: nowIso()
    };
    _assignments.push(rec);
    return rec;
}
async function assignLicenseBulk(payload) {
    await delay(120);
    const { licenseId, installedOn, seatMode, requestId, items } = payload;
    if (!licenseId) throw new Error("licenseId is required");
    if (!Array.isArray(items)) throw new Error("items is required");
    // Bulk idempotency (อย่างง่าย)
    if (_bulkSeen.has(requestId)) {
        // คืนผลว่าไม่มีการทำซ้ำ (เพื่อไม่กิน seat เพิ่ม)
        return {
            ok: true,
            assigned: 0,
            failed: []
        };
    }
    _bulkSeen.add(requestId);
    const license = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getLicenseById"])(licenseId);
    if (!license) {
        return {
            ok: false,
            assigned: 0,
            failed: items.map((it)=>({
                    employeeId: it.employeeId,
                    reason: "license-not-found"
                }))
        };
    }
    const perType = asPerType(license.licenseModel);
    if (perType === "per_device") {
        // payload นี้ไม่มี deviceId ต่อรายการ → ไม่รองรับ per-device
        // สามารถเปลี่ยนเป็นรองรับได้ถ้าเพิ่มฟิลด์ deviceId ต่อ item
        return {
            ok: false,
            assigned: 0,
            failed: items.map((it)=>({
                    employeeId: it.employeeId,
                    reason: "device-required"
                }))
        };
    }
    // เตรียมชุดผู้ที่มีสิทธิ์ (ไม่นับ exception)
    const candidates = items.filter((it)=>!it.exception).map((it)=>it.employeeId);
    // กรอง duplicate ที่มีอยู่แล้ว
    const dedup = candidates.filter((employeeId)=>{
        const existed = _assignments.find((a)=>a.licenseId === licenseId && a.employeeId === employeeId && a.status === "active");
        return !existed;
    });
    const needSeats = dedup.length;
    const available = license.available ?? license.total - (license.inUse ?? 0);
    // all-or-nothing: ถ้าที่นั่งไม่พอ → fail ทั้งชุด
    if (seatMode === "all-or-nothing" && available < needSeats) {
        const notEnough = dedup.map((employeeId)=>({
                employeeId,
                reason: "seat-insufficient"
            }));
        const failed = [
            ...items.filter((it)=>it.exception).map((it)=>({
                    employeeId: it.employeeId,
                    reason: it.exception.reason || "exception"
                })),
            ...notEnough,
            ...candidates.filter((employeeId)=>!dedup.includes(employeeId)).map((employeeId)=>({
                    employeeId,
                    reason: "duplicate"
                }))
        ];
        return {
            ok: false,
            assigned: 0,
            failed
        };
    }
    // partial (best-effort)
    const failed = [
        ...items.filter((it)=>it.exception).map((it)=>({
                employeeId: it.employeeId,
                reason: it.exception.reason || "exception"
            })),
        ...candidates.filter((employeeId)=>!dedup.includes(employeeId)).map((employeeId)=>({
                employeeId,
                reason: "duplicate"
            }))
    ];
    let assigned = 0;
    const toRollback = [];
    for (const employeeId of dedup){
        try {
            // ตรวจ seat เหลือ ณ ขณะนี้
            const licNow = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getLicenseById"])(licenseId);
            const availNow = licNow?.available ?? (licNow?.total || 0) - (licNow?.inUse || 0);
            if (availNow <= 0) throw new Error("seat-insufficient");
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["consumeSeat"])(licenseId, 1);
            const rec = {
                id: `asg-${Date.now()}-${Math.floor(Math.random() * 100000)}`,
                licenseId,
                employeeId,
                deviceId: null,
                status: "active",
                effectiveAt: installedOn || nowIso(),
                createdAt: nowIso()
            };
            _assignments.push(rec);
            toRollback.push(rec);
            assigned += 1;
        } catch (e) {
            failed.push({
                employeeId,
                reason: e?.message || "assign-failed"
            });
            if (seatMode === "all-or-nothing") {
                // rollback ทั้งหมดที่ทำไปแล้ว
                for (const r of toRollback){
                    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["releaseSeat"])(r.licenseId, 1);
                    r.status = "revoked";
                }
                return {
                    ok: false,
                    assigned: 0,
                    failed
                };
            }
        }
    }
    const ok = seatMode === "all-or-nothing" ? failed.length === 0 : true;
    return {
        ok,
        assigned,
        failed
    };
}
async function assignMany(params) {
    const results = [];
    const toRollback = [];
    for (const it of params.items){
        try {
            const rec = await assignLicense({
                licenseId: it.licenseId,
                employeeId: params.employeeId,
                deviceId: it.deviceId,
                idempotencyKey: it.idempotencyKey
            });
            results.push({
                licenseId: it.licenseId,
                status: "success",
                assignmentId: rec.id
            });
            toRollback.push(rec);
        } catch (e) {
            results.push({
                licenseId: it.licenseId,
                status: "error",
                message: e?.message || "assign failed"
            });
            if (params.allOrNothing) {
                for (const r of toRollback){
                    await rollbackAssignment(r);
                }
                return results;
            }
        }
    }
    return results;
}
async function unassignLicense(assignmentId) {
    await delay(100);
    const idx = _assignments.findIndex((a)=>a.id === assignmentId);
    if (idx < 0) return;
    const rec = _assignments[idx];
    if (rec.status !== "active") return;
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["releaseSeat"])(rec.licenseId, 1);
    _assignments[idx] = {
        ...rec,
        status: "revoked"
    };
}
async function getAssignmentsByEmployee(employeeId) {
    await delay(100);
    return _assignments.filter((a)=>a.employeeId === employeeId);
}
async function rollbackAssignment(rec) {
    if (rec.status === "active") {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["releaseSeat"])(rec.licenseId, 1);
        rec.status = "revoked";
    }
}
function delay(ms) {
    return new Promise((res)=>setTimeout(res, ms));
}
const ASSIGNED_TO = [
    {
        employeeId: "1",
        employeeName: "Puttaraporn Jitpranee",
        expiryDate: "2025-01-01",
        assignedDate: "2025.1",
        softwareId: "SW-3DSMAX",
        softwareName: "3ds Max",
        department: "Information Technology",
        status: "Active"
    }
];
async function getAssignedTo(softwareId) {
    return ASSIGNED_TO.filter((row)=>row.softwareId === softwareId);
}
async function getAssignedToFilters(softwareId) {
    const scoped = ASSIGNED_TO.filter((row)=>row.softwareId === softwareId);
    const employees = Array.from(new Set(scoped.map((r)=>r.employeeName).filter((v)=>typeof v === "string"))).sort();
    const departments = Array.from(new Set(scoped.map((r)=>r.department).filter((v)=>typeof v === "string"))).sort();
    const statuses = Array.from(new Set(scoped.map((r)=>r.status).filter((v)=>v !== undefined) // narrow เป็น LicenseStatus
    )).sort();
    const names = Array.from(new Set(scoped.map((r)=>r.softwareName).filter((v)=>typeof v === "string") // narrow เป็น string[]
    )).sort();
    const softwareName = names.length > 0 ? names[0] : "-";
    return {
        employees,
        departments,
        statuses,
        softwareName
    };
}
}),
"[project]/apps/web/services/history.mock.ts [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MOCK_HISTORY",
    ()=>MOCK_HISTORY,
    "getHistoryBySoftware",
    ()=>getHistoryBySoftware
]);
const MOCK_HISTORY = [
    {
        id: "h1",
        detail: "License renewed",
        actor: "Admin",
        timestamp: "2025-12-01",
        action: "assign"
    },
    {
        id: "h2",
        detail: "Policy updated",
        actor: "Security Team",
        timestamp: "2025-08-15",
        action: "assign"
    }
];
async function getHistoryBySoftware(id) {
    return MOCK_HISTORY;
}
}),
"[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (client reference proxy) <module evaluation>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
// This file is generated by next-core EcmascriptClientReferenceModule.
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server.js [app-rsc] (ecmascript)");
;
const __TURBOPACK__default__export__ = (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call the default export of [project]/apps/web/components/detail/LicenseDetail.tsx <module evaluation> from the server, but it's on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/apps/web/components/detail/LicenseDetail.tsx <module evaluation>", "default");
}),
"[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (client reference proxy)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
// This file is generated by next-core EcmascriptClientReferenceModule.
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server.js [app-rsc] (ecmascript)");
;
const __TURBOPACK__default__export__ = (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call the default export of [project]/apps/web/components/detail/LicenseDetail.tsx from the server, but it's on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/apps/web/components/detail/LicenseDetail.tsx", "default");
}),
"[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$detail$2f$LicenseDetail$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__$3c$module__evaluation$3e$__ = __turbopack_context__.i("[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (client reference proxy) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$detail$2f$LicenseDetail$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__ = __turbopack_context__.i("[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (client reference proxy)");
;
__turbopack_context__.n(__TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$detail$2f$LicenseDetail$2e$tsx__$5b$app$2d$rsc$5d$__$28$client__reference__proxy$29$__);
}),
"[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>LicenseDetailPage
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react-jsx-dev-runtime.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$ui$2f$BackButton$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/components/ui/BackButton.tsx [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$api$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/api/navigation.react-server.js [app-rsc] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/node_modules/next/dist/client/components/navigation.react-server.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/services/licenses.service.mock.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$assign$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/services/assign.service.mock.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$history$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/services/history.mock.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$detail$2f$LicenseDetail$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/apps/web/components/detail/LicenseDetail.tsx [app-rsc] (ecmascript)");
;
;
;
;
;
;
;
async function LicenseDetailPage({ params }) {
    // ❌ เดิม: const { id } = await params;
    //   params เป็น sync object
    const { id } = await params;
    // 1) โหลดข้อมูลหลัก (license)
    const license = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$licenses$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getLicenseById"])(id);
    if (!license) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["notFound"])();
    }
    // 2) โหลดข้อมูลอื่น ๆ แบบขนาน
    const [assigned, _assignedFilters, history] = await Promise.all([
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$assign$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getAssignedTo"])(id),
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$assign$2e$service$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getAssignedToFilters"])(id),
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$services$2f$history$2e$mock$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getHistoryBySoftware"])(id)
    ]);
    // 3) แปลง assigned ให้เป็น flat array ของ InstallationRow
    const installations = Array.isArray(assigned) && Array.isArray(assigned[0]) ? assigned.flat() : assigned;
    // 4) breadcrumbs ของ License
    const breadcrumbs = [
        {
            label: "Software Inventory",
            href: "/software/inventory"
        },
        {
            label: "License Management",
            href: "/software/license-management"
        },
        {
            label: license.softwareName,
            href: `/software/license-management/${id}`
        }
    ];
    return /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])("div", {
        className: "p-2",
        children: [
            /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$ui$2f$BackButton$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"], {}, void 0, false, {
                fileName: "[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx",
                lineNumber: 47,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$apps$2f$web$2f$components$2f$detail$2f$LicenseDetail$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"], {
                item: license,
                installations: installations,
                history: history,
                breadcrumb: breadcrumbs
            }, void 0, false, {
                fileName: "[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx",
                lineNumber: 48,
                columnNumber: 7
            }, this)
        ]
    }, void 0, true, {
        fileName: "[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx",
        lineNumber: 46,
        columnNumber: 5
    }, this);
}
}),
"[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx [app-rsc] (ecmascript, Next.js Server Component)", ((__turbopack_context__) => {

__turbopack_context__.n(__turbopack_context__.i("[project]/apps/web/app/(app)/software/license-management/[id]/page.tsx [app-rsc] (ecmascript)"));
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__aab754a1._.js.map