# --- VCS & IDEs ---
.git
.gitignore
.gitattributes
.idea
.vscode

# --- Package managers cache ---
**/node_modules
.npm
.pnpm-store
.yarn
.yarn/cache
.yarn/install-state.gz

# --- Build outputs / caches ---
**/.turbo
**/.next
**/dist
**/build
**/out
**/.cache
**/coverage
**/tmp
**/.eslintcache
**/.tsbuildinfo

# --- Logs ---
**/*.log
npm-debug.log

# --- OS junk ---
.DS_Store
Thumbs.db

# --- Env & secrets (อย่าส่งขึ้น image) ---
.env
.env.*
**/*.env
**/*.env.*

# --- Large or irrelevant directories at root ---
tools           # ถ้าไม่มีใช้ใน build ให้ ignore
configs/**/*.md # เอกสารที่ไม่จำเป็นต่อ build
